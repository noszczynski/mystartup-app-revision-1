import React, { FC } from 'react';
import Page from 'components/Page';
import { ArticleList } from '../components';

const News: FC = () => (
  <Page>
    <ArticleList />
  </Page>
);

export default News;
